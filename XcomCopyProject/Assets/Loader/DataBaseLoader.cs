using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataBaseLoader : Loader
{

    // Start is called before the first frame update
    public override void Load()
    {

        base.Load();
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
